# FaithNode Wiki Pages

Репозиторий вики-страниц для [FaithNode](https://faithnode.host). Здесь размещается контент, создаваемый нашим
сообществом.

## 📖 О репозитории

Это хранилище вики-страниц хостинг провайдера FaithNode. Любой пользователь может вносить свой вклад, добавляя новые
статьи и улучшая существующий контент.

В CHANGELOG.md написаны все изменения, а так же задачи на будущее

## 🚀 Как начать

1. **Форк** этого репозитория
2. **Создайте** новую ветку для вашей статьи
3. **Добавьте** содержимое в формате Markdown
4. **Отправьте** Pull Request

## 📝 Требования

- Используйте Markdown формат
- Добавляйте описательные названия файлов
- Соблюдайте структуру папок по категориям
- Проверяйте орфографию и грамматику

## 📂 Структура

```
/
├── pages/               # Страницы вики
│   ├── metadata.yml     # Опционально, дополнительные метаданные группы
│   └── example.mdx      # Шаблон форматирования текста
├── config/
│   ├── metadata.yml     # Метаданные страницы по умолчанию
│   └── variables.yml    # Глобальные переменные
└── assets/              # Изображения и файлы
```

### Нюансы структуры `pages`

- URL формируется из структуры папок, без расширения: `pages/dir/somepage.mdx => URL /dir/somepage`
- Файлы `index.mdx|index.md` не входят в финальный путь: `pages/terms/index.mdx => URL /terms`
- Для сортировки файлов и групп можно использовать название папок в формате `[<number>]`, например
  `pages/[0]/terms/oferta.mdx => URL /terms/oferta`.
- Для скрытия папки из пути можно использовать формат: `(<dirname>)`, например
  `pages/(hidden)/example.mds => URL /example`.

## ✅ Правила участия

- Контент должен быть полезным и актуальным
- Не допускаются спам и реклама
- Разрешается оставить свое авторство на конце страницы в таком ввиде:
  `Автор статьи: [NICK](https://github.com/NICK "blank").`

## 📧 Контакты

По вопросам обращайтесь через Issues или в [Telegram @watereks](https://t.me/watereks).
