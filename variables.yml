##
# Файл для хранения общих переменных
#
# Использование: `{{ organization.name }}`, `{{ pages.index }}`
#
# Можно использовать переменные из app.yml, variables.yml
#
# Заполняется один раз при старте приложения
##

# Локализация страниц
pages:
  index: 'Навигатор'